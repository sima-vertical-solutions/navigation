# Build dependencies for the nav2 packages this repository ships, installed by
# packaging/Dockerfile.build.
#
# Source of truth is the bdeps list in swsoc-elxr-config
# boards/arm64/simaai/packages/vdp-navigation/vdp-navigation.yaml, with two
# edits:
#
#   * the :arm64 / :all suffixes are dropped. That recipe cross-compiles from
#     x86 against an arm64 sysroot, so every entry had to name an architecture.
#     This build is native on ubuntu-24.04-arm, so there is no multiarch to
#     disambiguate.
#
#   * the toolchain is added back. The eLxr recipe assumed a cross toolchain
#     was already in the builder image, so build-essential, dpkg-dev,
#     pkg-config and git appear nowhere in its bdeps -- and dpkg-dev in
#     particular is not optional here: dpkg-shlibdeps and dpkg-architecture
#     come from it and this script cannot produce a deb without them.
#
# `ros2` was also in that list. It is not here, because it does not come from
# apt any more -- see the Vulcan install in packaging/Dockerfile.build.
#
# Every entry was checked against
# dists/bookworm/main/binary-arm64/Packages.gz, not packages.debian.org, which
# answers 200 for names that do not exist.

# --- toolchain, absent from the eLxr list because the sysroot supplied it ---
build-essential
ca-certificates
cmake
curl
dpkg-dev
git
pkg-config

# --- libraries the nav2 packages link against ---
libassimp-dev
libboost-filesystem-dev
libboost-program-options-dev
libboost-python-dev
libboost-serialization-dev
libboost-system-dev
libboost-thread-dev
libcap-dev
# nav2_smac_planner's smoother and nav2_constrained_smoother optimise with Ceres.
libceres-dev
libconsole-bridge-dev
libeigen3-dev
libfcl-dev
libfmt-dev
libfreetype-dev
libgeographiclib-dev
libglew-dev
libgl-dev
libgl1-mesa-dev
libglu1-mesa-dev
# nav2_map_server loads and saves map images through GraphicsMagick.
libgraphicsmagick++1-dev
liblapack-dev
liblttng-ust-dev
# nav2_route indexes its graph with nanoflann and parses it with nlohmann-json.
libnanoflann-dev
nlohmann-json3-dev
# nav2_smac_planner's Hybrid-A* uses OMPL's Dubins/Reeds-Shepp state spaces.
libompl-dev
libopenblas-dev
libopencv-dev
liborocos-kdl-dev
libprotobuf-dev
libspdlog-dev
libsqlite3-dev
libsuitesparse-dev
libtinyxml2-dev
libyaml-cpp-dev
libzmq3-dev
zlib1g-dev

# --- xtensor/xsimd, which nav2_mppi_controller is written against ---
libxsimd-dev
libxtensor-dev
xtl-dev

# --- rviz and Qt ---
#
# nav2_bringup build_depends the `navigation2` metapackage, whose exec_depends
# list includes nav2_rviz_plugins, and colcon treats an exec_depend as an
# ordering edge -- so --packages-up-to nav2_bringup builds the rviz plugins
# whether or not the board ever displays anything. They need the rviz_rendering
# headers, which need Ogre, and the panel needs Qt5.
libogre-1.12-dev
libqt5opengl5-dev
libqt5svg5-dev
qt5-qmake
qtbase5-dev
libx11-dev
libxaw7-dev
libxcomposite-dev
libxcursor-dev
libxext-dev
libxi-dev
libxinerama-dev
libxrandr-dev
libxrender-dev
libxt-dev

# --- python build tooling ---
#
# colcon, vcstool and the rosidl generators come from pip in
# packaging/Dockerfile.build, the way the eLxr recipe installed them; these are
# the pieces Debian does package.
python3
python3-catkin-pkg
python3-dev
python3-empy
python3-lark
python3-numpy
python3-pip
python3-pkg-resources
python3-pyparsing
python3-setuptools
python3-venv
python3-wheel
